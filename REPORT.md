# 💡 รายงาน: หลักการทำงานและแนวคิดสำคัญ (Technical Concepts)

เพื่อให้เข้าใจการทำงานของโปรเจกต์นี้ในเชิงลึกยิ่งขึ้น นี่คือคำอธิบายแนวคิดสำคัญ 4 ข้อที่เกี่ยวข้องกับเทคโนโลยีที่ใช้:

### 1. JS Engine vs. Runtime (Frontend vs. Backend)

- **Frontend (Browser Runtime):** โค้ด React ของเราทำงานอยู่ใน "Browser Runtime" ซึ่งเป็นสภาพแวดล้อมที่เบราว์เซอร์ (เช่น Chrome, Firefox) จัดเตรียมไว้ให้ ภายในประกอบด้วย **JavaScript Engine** (เช่น V8 Engine) ที่ทำหน้าที่ประมวลผลโค้ด และยังมี **Web APIs** (เช่น `document`, `window`, `fetch`) ให้ JavaScript เรียกใช้เพื่อโต้ตอบกับหน้าเว็บและเบราว์เซอร์ได้
- **Backend (Node.js Runtime):** โค้ด Express.js ของเราทำงานอยู่ใน "Node.js Runtime" ซึ่งเป็นสภาพแวดล้อมที่ทำให้ JavaScript สามารถทำงานนอกเบราว์เซอร์ได้ ถึงแม้จะใช้ **V8 Engine** ตัวเดียวกัน แต่ Node.js จะมี APIs ที่ต่างออกไป เช่น `fs` (File System) สำหรับจัดการไฟล์ หรือ `http` สำหรับสร้างเซิร์ฟเวอร์ ทำให้ JavaScript สามารถทำงานในฝั่งเซิร์ฟเวอร์ได้

### 2. Virtual DOM (DOM เสมือน)

- **ปัญหา:** การแก้ไข DOM (โครงสร้างหน้าเว็บ) โดยตรงนั้นช้าและใช้ทรัพยากรสูง หากมีข้อมูลเปลี่ยนแปลงเพียงเล็กน้อย แต่ต้องวาดหน้าจอใหม่ทั้งหมด จะทำให้เว็บกระตุกได้
- **ทางออกของ React:** React ได้สร้างสิ่งที่เรียกว่า **Virtual DOM (VDOM)** ซึ่งเป็นสำเนาของ DOM จริงที่เก็บไว้ในหน่วยความจำ เมื่อมีการเปลี่ยนแปลง State (เช่น การเพิ่มโน้ตใหม่) React จะ:
  1. สร้าง VDOM ชุดใหม่ขึ้นมา
  2. เปรียบเทียบ VDOM ชุดใหม่กับชุดเก่า (กระบวนการนี้เรียกว่า "Diffing") เพื่อหาจุดที่แตกต่างกัน
  3. เมื่อพบจุดที่แตกต่าง React จะอัปเดตเฉพาะส่วนนั้นๆ ใน DOM จริงเพียงครั้งเดียว
- **ผลลัพธ์:** วิธีนี้ทำให้การอัปเดตหน้าจอมีประสิทธิภาพและรวดเร็วกว่าการแก้ไข DOM จริงโดยตรงมาก

### 3. HTTP/HTTPS Request & Response

- **วงจรเมื่อกดปุ่ม "Submit" (บันทึกโน้ต):** เมื่อผู้ใช้กรอกข้อมูลและกดปุ่มบันทึก จะเกิดวงจร Request-Response ขึ้นดังนี้:
  1. **Request (คำขอ):** Frontend จะส่งคำขอประเภท `POST` ไปยัง API ของ Backend พร้อมกับแนบข้อมูลโน้ตไปใน Body นอกจากนี้ยังมีการส่ง **Headers** ที่สำคัญไปด้วย ได้แก่:
     - `Content-Type: application/json` (เพื่อบอก Backend ว่าข้อมูลที่ส่งไปเป็นฟอร์แมต JSON)
     - `Authorization: <รหัสลับ>` (เพื่อส่ง Token ยืนยันตัวตนให้ Backend ตรวจสอบสิทธิ์)
  2. **Response (การตอบกลับ):** Backend รับคำขอ, ตรวจสอบ Token, บันทึกข้อมูลลงฐานข้อมูล (SQLite) และส่งผลลัพธ์ตอบกลับมา เช่น สถานะ `201 Created` (สร้างสำเร็จ) กลับมาให้ Frontend รับทราบเพื่ออัปเดตหน้าจอ
- **ทำไมต้องเป็น HTTPS?:** การส่งข้อมูลด้วย **HTTP** ธรรมดาจะส่งเป็นข้อความปกติ (Plain Text) หากมีคนดักฟังข้อมูลระหว่างทาง เขาจะเห็นทั้งข้อความในโน้ตและ `Authorization Header` ของเราทั้งหมด แต่ระบบจริงจำเป็นต้องใช้ **HTTPS** (Secure) เพราะมันจะ **เข้ารหัส (Encrypt)** ข้อมูลและ Headers ทั้งหมดก่อนส่ง ทำให้ผู้ไม่หวังดีไม่สามารถแอบขโมย Token หรือแอบดูข้อมูลโน้ตของเราได้

### 4. Environment Variables (ทำไม SECRET_TOKEN ต้องอยู่ฝั่ง Backend)

- **ทำไมต้องเก็บไว้ที่ Backend?:** Backend เป็นสภาพแวดล้อมที่ปลอดภัยและเราควบคุมได้ทั้งหมด การเก็บ `SECRET_TOKEN` ไว้ใน Environment Variable ของฝั่งเซิร์ฟเวอร์ ทำให้มีเพียงโค้ด Backend เท่านั้นที่เข้าถึงค่านี้ได้
- **อันตรายของการเก็บไว้ที่ Frontend:** โค้ดฝั่ง Frontend ทั้งหมด (JavaScript, HTML, CSS) จะถูกส่งไปให้ผู้ใช้ดาวน์โหลดและรันบนเบราว์เซอร์ของเขา **หากเราเขียน `SECRET_TOKEN` ไว้ในโค้ด React (Frontend) ใครก็ตามก็สามารถกด "View Page Source" หรือเปิด Developer Tools เพื่อดูโค้ดและขโมย Token ของเราไปได้ทันที** เมื่อได้ Token ไปแล้ว เขาก็จะสามารถส่งคำสั่งมายิง API ของเราโดยตรง (เช่น สั่งลบโน้ตทั้งหมด) โดยไม่ต้องผ่านหน้าเว็บของเราเลย ซึ่งเป็นอันตรายอย่างยิ่ง